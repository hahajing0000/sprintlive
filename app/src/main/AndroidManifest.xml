<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baweigame.sprintnba">

    <application
        android:name=".app.SprintNBAApp"
        android:allowBackup="false"
        android:icon="@mipmap/nba_default"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/nba_default"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.LoggerTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.GuidePageActivity">

        </activity>
        <activity android:name=".ui.SplashActivity"></activity>
        <activity android:name=".ui.HomeActivity" />
    </application>

</manifest>